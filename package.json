{
  "name": "osrs-clone-codex",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone static codex for the OSRS Clone project.",
  "scripts": {
    "sync:data": "node scripts/sync-data.js",
    "check": "node scripts/validate-data.js",
    "build": "node scripts/build.js",
    "serve": "npm run build && start \"\" http://localhost:5520/osrs-clone-codex/ && python -m http.server 5520 -d dist"
  }
}
